describe("Pruebas <DemoComponent/>", () => {
    
    test('Esta prueba no debe de fallar', () => {
    
    
        const message = 'Hola mundo'
    
        const messageTrim = message.trim();
    
        expect(message).toBe(messageTrim)
    
    
    })

})
